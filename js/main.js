var firstnName = prompt("What is your first name");

var lastName = prompt("What is your last name");

var age = prompt("How old are you (numeric value)");

var month = age * 12;

var day = age * 365;

alert("Hello " + firstnName + " " + lastName +", " + "it looks like you are " + age + " years old. That means you are " + month + " months old and " + day + " days old.... You old as hell..." );
